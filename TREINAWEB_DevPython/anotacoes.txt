
Preparando ambiente no linux
sudo snap install pycharm-community --classic

Tipagem dinâmica

Os blocos de código são definidos pela identação

O input sempre captura uma string, então para alterar o tipo precisa ser feita a  int(input("digite a idade"))

A atribuição multipla permite em uma linha atribuir diferentes valores a diferentes variaveis

// é um operador para retornar a parte inteira da divisão
% é o operador para retornar o resto da divisão inteira
** é uma exponenciação

if ():

elif ():

else:

while
else

for
else #usado para indicar o fim do bloco, está em desuso

#POO

class Usuario:
    def __init__(self, nome, idade, sobrenome): #metodo construtor
        self.nome = nome
        self.idade = idade
        self.sobrenome = sobrenome

from usuario import Usuario #para importar um metodo do arquivo usuario

#import usuario #importa todos os metodos do arquivo

usuario = Usuario("Iuri", 31, "Salgado") #instanciar

print(f"{usuario.nome} {usuario.sobrenome}, {usuario.idade}")




TW-DesenvolvedorPython - 1 Escopos e namespaces - Python POO
